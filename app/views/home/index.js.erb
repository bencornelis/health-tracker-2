$("#meals").html("<%= j render('meals/meals') %>");
$("#meal-sorting-options").html("<%= j render('meals/meal_sorting_options') %>");
$('.pagination a').click(function () {
  $.getScript(this.href);
  return false;
});
$('#datetimepicker2').datetimepicker({
  format: 'YYYY-MM-DD'
});
